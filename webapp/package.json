{
  "name": "tikovm-webapp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "setup": "npm --prefix ../clients/node install && npm --prefix ../clients/node run build && npm install",
    "build": "npm run build -w web && npm run build -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web",
    "test": "npm --prefix ../clients/node run build && npm test -w server",
    "start": "node server/dist/index.js",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "esbuild@0.21.5": true
  }
}
